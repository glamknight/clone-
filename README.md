# Netflix Clone

A Netflix-style homepage built from scratch using **Bun**, **React**, and **Vite**.

## 🚀 Getting Started

### Prerequisites
Since the project was generated in an environment without Bun installed, you must install it first to run the project.

- **Bun**: [Install Bun](https://bun.sh/) (Windows/macOS/Linux)

### Installation
Once Bun is installed, run the following command in the project directory:

```bash
bun install
```

### Running Development Server
Start the development server with:

```bash
bun run dev
```
The application will be available at `http://localhost:5173`.

### Building for Production
To build the project for production:

```bash
bun run build
```

To preview the production build:

```bash
bun run preview
```

## 📂 Project Structure

```
netflix-clone/
├── public/          # Static assets
│   └── index.html   # Main HTML entry point
├── src/
│   ├── components/  # React components
│   │   ├── Header.jsx   # Top navigation bar
│   │   ├── Hero.jsx     # Featured content banner
│   │   ├── MovieRow.jsx # Horizontal scrolling categories
│   │   └── Footer.jsx   # Page footer
│   ├── styles/      # Component styles
│   ├── data/        # Mock data (movies.js)
│   ├── App.jsx      # Main layout
│   └── main.jsx     # Entry point
├── package.json     # Project configuration and scripts
└── vite.config.js   # Vite configuration
```

## 🎨 Features
- **Sticky Header**: Navigation bar that changes background on scroll.
- **Hero Banner**: Featuring a prominent movie with Play/My List buttons.
- **Horizontal Scrolling**: Scrollable rows for different movie categories.
- **Hover Effects**: Interactive scale animations on movie cards.
- **Responsive Design**: Adapts for mobile and desktop screens.

## 🛠 Tech Stack
- **Bun**: Fast JavaScript runtime & package manager.
- **React**: UI Library.
- **Vite**: Next Generation Frontend Tooling.
- **CSS**: Custom styling with Flexbox and Grid.

---
*Generated by Antigravity*
